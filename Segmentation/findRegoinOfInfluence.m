function roi = findRegoinOfInfluence(ilabel, param)
% 
% roi = findRegoinOfInfluence(label, param)
%
% description:
%    for a labeld image ilabel returns the regoins of influence 
%    not exceeding a maximal distance specified in param
%
% input:
%    ilabel    labeled image
%    param     (optional) parameter struct with entries
%              distance.max  maximal distance for influence zone
%
% output: 
%    roi       labeld imaged including the regoin of influecnes of the labels in ilabel
%
% See also: 


end