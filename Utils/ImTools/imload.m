function img = imload(fname, specs)
%
% img = imload(fname, specs)
%
% description: loads image data defined via specs and returns an pqlct array
%
% input: 
%     fname    a file, directory or an tagged image name format
%     specs    image specifications as struct or list of name value pairs
%
% output:
%     img      the image data as numeric array
%
% See also: imread





end